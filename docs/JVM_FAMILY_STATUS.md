# JVM family implementation status

Java, Kotlin, and Scala native adapters are implemented on `agent/expand-jvm-family` and wired into the VerificationForge CLI. They are intentionally not marked complete in the master tracker until the dedicated real-toolchain workflow and the full existing VerificationForge CI suite pass on the exact implementation head.
